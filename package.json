{
  "name": "rxjs-dynamodb-client",
  "version": "1.0.0",
  "description": "Lightweight dynamoDb client built on top of RxJS",
  "main": "index.js",
  "scripts": {
    "deploy": "npm version patch -m 'Bump %s' && git push origin master && npm publish",
    "test": "NODE_ENV=test ./node_modules/.bin/_mocha **.spec.js **/**.spec.js --recursive --watch --timeout 5000",
    "coverage": "NODE_ENV=test ./node_modules/.bin/istanbul cover -x '**/**.spec.js' -x '**.spec.js' -x 'testing/**' ./node_modules/.bin/_mocha -- **.spec.js **/**.spec.js --recursive"
  },
  "author": "Felipe Rohde",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.568.0",
    "cuid": "^2.1.6",
    "lodash": "^4.17.15",
    "rxjs": "6.5.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/feliperohdee/rxjs-dynamodb-client"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "istanbul": "^0.4.5",
    "mocha": "^5.1.1",
    "sinon": "^4.5.0",
    "sinon-chai": "^3.0.0"
  }
}
